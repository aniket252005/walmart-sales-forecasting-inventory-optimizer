# Dashboard only — lightweight for Streamlit Cloud deployment
# Heavy ML packages (prophet, pmdarima, xgboost, scikit-learn)
# are only needed to run main.py locally, not to serve the dashboard.

pandas>=2.1.0,<2.3
numpy>=1.26.0,<2.0
plotly>=5.18.0
streamlit>=1.31.0
PyYAML>=6.0
